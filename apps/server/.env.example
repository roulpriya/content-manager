OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# GitHub integration — enables automatic daily GitHub activity posts
# Get a token at https://github.com/settings/tokens (read:user + repo scopes)
GITHUB_TOKEN=
GITHUB_USERNAME=  # optional; auto-resolved from token if omitted
